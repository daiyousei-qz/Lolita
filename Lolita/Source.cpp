#include <any>
#include <string_view>
#include <cassert>

int main()
{
	using namespace std;
	// auto config = ParseConfig(s);
	// auto parsing_info = BootstrapParser(*config);
	// auto code = GenerateParser(parsing_info);

	system("pause");
}